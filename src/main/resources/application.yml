spring:
  # dev
  profiles:
    active: dev
  application:
    name: mysecurity

  # DB/JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# security
#  security:
#    user:
#      # 테스트용 사용자 이름
#      name: user
#      password: password
#      roles: USER

  server:
    port: 8080